{
  "name": "opencode-annotate-plugin",
  "private": true,
  "workspaces": ["plugin", "client", "shared"],
  "scripts": {
    "build": "bun run --filter @opencode-annotate/plugin build && bun run --filter @opencode-annotate/client build",
    "pack:dry-run": "bun run --filter @opencode-annotate/plugin pack:dry-run && bun run --filter @opencode-annotate/client pack:dry-run"
  },
  "dependencies": {
    "@types/bun": "^1.3.14",
    "zod": "^4.4.3"
  }
}
